using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 플레이어가 감지되면 조종선을 활성화하고 싶다.
// 공격할 땐 조종선을 비활성화하고 싶다.
// 플레이어가 감지되지 않으면 조종선을 비활성화하고 싶다.
public class KANG_TurretBeam : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
